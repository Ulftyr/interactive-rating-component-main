const reviewItems = document.querySelectorAll('card__review-item')
const buttonSubmit = document.querySelector('card__submit')
const span = document.querySelector('choice')

reviewItems.forEach(e => {
	console.log(e.target)
})
